<div class="landing-page">
    <aside id="filters">

    </aside>
    <section class="restaurant-list">
        {{#each restaurants}}
            {{>restaurantCard this}}
        {{/each}}
    </section>
</div>