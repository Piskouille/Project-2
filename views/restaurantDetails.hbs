<div>
  <h2>{{restaurant.name}}</h2>
  <div>
    <img src="{{restaurant.image}}" />
    {{#each restaurant.address}}
      <li>{{this}}</li>
    {{/each}}
      <p>{{restaurant.foodTypes}}</p>
    <p>{{restaurant.priceRating}}</p>
    <a></a>
  </div>
</div>